#ifndef INC_GRAPH
#define INC_GRAPH

#include <iostream>
#include <bits/stdc++.h>

using namespace std;

#define INF 99999

class CGraph
{
   /* In the graph, we need to store node and weight pair for every edge */
//   list<pair<int,int>> *adj;

public :
   /* constructor */
   CGraph(int nbNodes);

//   void addEdge(int begin, int end, int distance);

//   void shortestPath(int from);
};

#endif
